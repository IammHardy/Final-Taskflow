default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  primary:
    <<: *default
    host: localhost
    username: yusuf
    password: yusuf123
    database: taskflow_development
  queue:
    <<: *default
    host: localhost
    username: yusuf
    password: yusuf123
    database: taskflow_development_queue
    migrations_paths: db/queue_migrate
  cache:
    <<: *default
    host: localhost
    username: yusuf
    password: yusuf123
    database: taskflow_development_cache
    migrations_paths: db/cache_migrate

test:
  primary:
    <<: *default
    host: localhost
    username: yusuf
    password: yusuf123
    database: taskflow_test

production:
  primary: &primary_production
    <<: *default
    url: <%= ENV["DATABASE_URL"] %>
  cache:
    <<: *primary_production
    migrations_paths: db/cache_migrate
  queue:
    <<: *primary_production
    migrations_paths: db/queue_migrate
  cable:
    <<: *primary_production
    migrations_paths: db/cable_migrate
